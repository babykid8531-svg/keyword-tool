import streamlit as st
import pandas as pd
from openai import OpenAI

# -----------------------------
# 기본 세팅
# -----------------------------
st.set_page_config(page_title="키워드 추천 및 네이버 글 자동 생성", layout="wide")
st.title("키워드 추천 및 분석받기")

client = OpenAI(api_key=st.secrets["OPENAI_API_KEY"])

# 세션 상태 초기화
if "analysis_done" not in st.session_state:
    st.session_state.analysis_done = False
if "base_keyword" not in st.session_state:
    st.session_state.base_keyword = ""
if "kw_table" not in st.session_state:
    st.session_state.kw_table = None
if "top10_df" not in st.session_state:
    st.session_state.top10_df = None
if "top3_keywords" not in st.session_state:
    st.session_state.top3_keywords = None


# -----------------------------
# 0. 글 작성 지침서 (프롬프트용)
# -----------------------------
GUIDELINE = """
너는 네이버 블로그 전문 작가이자 SEO 컨설턴트다.
아래 지침서를 항상 그대로 따르면서 글을 작성해라.

[지침서]

0️⃣ 이 지침서의 대원칙

- 구조는 항상 같다.
- 정보가 감정보다 먼저다.
- 독자는 “처음 가는 사람”이다.

1️⃣ 글 전체 고정 구조 (모든 글 공통)

[제목]
도입부(인사 + 요약)

① 이 공간/장소/서비스는 무엇인가요
② 언제·어떻게 이용하나요 (시간·요일·조건)
③ 내부 구성·이용 흐름·동선은 어떻게 되나요
④ 접근 방법·주차·교통은 어떤가요
⑤ 이런 사람에게 잘 맞아요

마무리
해시태그

2️⃣ 제목 지침

- 형식: 지역/대상 + 이름 + 핵심 정보 2~3개 + 총정리
- 핵심 정보 예: 운영시간, 주차, 위치, 이용방법, 요금, 예약
- 감성 단어, 후기형 표현, 추상적인 표현은 제목에 쓰지 않는다.

3️⃣ 도입부 지침 (4~5줄)

구성 예시:
- 안녕하세요! 로 시작
- 오늘은 [지역]에서 한 번쯤 궁금해질 만한 [대상 이름]을 정리했다고 밝힌다.
- 이 대상의 정체성을 한 줄로 설명.
- 운영시간, 이용 방법, 주차와 동선까지 한 번에 확인할 수 있게 정리했다고 안내.
- 처음 방문하는 사람에게 도움이 된다고 마무리.
도입부에는 감정이나 후기 표현을 넣지 않는다.

4️⃣ ① 이곳은 무엇인가요

- 정체성을 설명하고 신뢰를 확보하는 파트.
- 설명 → 배경 → 현재 모습 순서.
- 연도, 숫자, 사실 정보를 우선.
- 한 문단에 감정 표현은 한 문장만 허용.
- 감상문이 아니라 설명문/관찰 위주.

5️⃣ ② 이용 시간·조건

- 요일, 운영시간, 입장 마감 시간, 휴무일, 예외사항을 명확히.
- 단정적인 문장, 애매한 표현 사용 금지.
- 마지막에 주의사항/팁 1줄.

6️⃣ ③ 내부 구성·동선·이용 흐름

- 처음 가는 사람 기준.
- 어디서부터 시작하는지, 어떤 순서로 보면 좋은지, 전체 소요 시간.
- 사진 포인트/관람 포인트 위치.
- 마지막에 실전 팁 1줄 (시간대, 동선 등).

7️⃣ ④ 주차·교통·접근성

- 감정 없이 정보만 전달.
- 주차: 무료/유료, 가능 대수, 도보 거리.
- 대중교통: 주요 출발지 기준 노선, 하차 후 도보 시간.
- 마지막에 “접근성 종합 판단” 한 줄.

8️⃣ ⑤ 이런 사람에게 맞아요

- 무조건 추천이 아니라 “어떤 유형에게 적합한지” 정리.
- 예: 조용히 둘러보고 싶은 사람, 아이와 함께 오는 가족 등.
- “강력 추천” 같은 표현 사용 금지.

9️⃣ 마무리 문단

- 3문장 구조:
  ① 전체 요약
  ② 핵심 장점 정리
  ③ 부드러운 방문 유도 (질문형 1문장까지 허용)

🔟 해시태그

- 7~10개.
- 구성: 고유명사 2개 / 지역 관련 2개 / 카테고리 2개 / 정보형 키워드 2개.
- 감성 태그, 의미 없는 태그 사용 금지.

이 지침을 어기지 말고,
항상 “정보 > 감정, 처음 가는 사람 기준, 고정 구조 유지” 원칙을 지켜라.
"""


# -----------------------------
# 1. GPT로 연관 키워드 + 상위 10개 생성
# -----------------------------
def generate_keywords_with_gpt(base_keyword: str):
    """
    base_keyword 하나를 받아서
    ① 연관 키워드 50개(격자형)
    ② 상위 노출 가능 키워드 10개 (검색량·이유 포함)
    ③ 그 중에서 자동으로 선정한 TOP3
    를 반환한다.
    """

    prompt = f"""
너는 한국 네이버 블로그용 SEO 키워드 기획자다.

[입력 키워드]
{base_keyword}

1단계: 이 키워드를 중심으로, 실제 네이버에서 검색할 법한 연관 키워드 50개를 만들어라.
- 너무 일반적인 단어(여행, 맛집, 카페)만 쓰지 말고
- "지역명 + 구체적 의도" 형태 위주로 만든다.
- 예: 전주 덕진공원 연꽃, 덕진공원 연꽃 시즌, 전주 덕진공원 사진 명소 등

2단계: 그 중에서 블로그 상위 노출 가능성이 높은 키워드 10개를 골라,
각각에 대해
- 키워드
- 검색량 (높음/중상/중 정도의 3단계)
- 이유 (한 줄)
을 표로 정리해라.

3단계: 위 10개 중에서 블로그 글의 핵심 키워드로 사용할 것 3개를 자동으로 선정해라.
- 1개는 메인 키워드 (가장 대표적인 검색어)
- 1개는 정보형/시즌형 키워드
- 1개는 롱테일 키워드(사진, 후기, 코스 등 행동 의도 포함)
이 되도록 골라라.

출력 형식은 반드시 아래 JSON 형식만 사용해라.

{{
  "all_keywords": ["키워드1", "키워드2", ... 50개],
  "top10": [
    {{"keyword": "키워드A", "volume": "높음", "reason": "이유"}},
    ...
  ],
  "top3": ["메인키워드", "정보형키워드", "롱테일키워드"]
}}
"""

    response = client.chat.completions.create(
        model="gpt-4.1-mini",
        messages=[
            {"role": "system", "content": "너는 SEO 전문 키워드 분석가다. 반드시 JSON만 반환해라."},
            {"role": "user", "content": prompt},
        ],
        temperature=0.5,
    )

    import json

    raw = response.choices[0].message.content.strip()
    data = json.loads(raw)

    # 1번 영역: 연관 키워드 50개를 5열 격자로 만들기
    all_kw = data["all_keywords"][:50]
    cols = 5
    rows = (len(all_kw) + cols - 1) // cols
    grid = []
    for r in range(rows):
        row = all_kw[r * cols : (r + 1) * cols]
        # 빈 칸 채우기
        while len(row) < cols:
            row.append("")
        grid.append(row)
    kw_table = pd.DataFrame(
        grid,
        columns=[f"{c}" for c in range(cols)],
    )

    # 2번 영역: 상위 노출 가능 10개 표
    top10_df = pd.DataFrame(data["top10"])

    # 3번: 자동 선정 키워드 3개
    top3 = data["top3"][:3]

    return kw_table, top10_df, top3


# -----------------------------
# 2. GPT로 네이버 블로그 글 생성
# -----------------------------
def generate_naver_post(base_keyword: str, selected_keywords: list[str]) -> str:
    """
    base_keyword와 자동 선정된 3개 키워드를 넣으면
    네이버 블로그용 글을 완성된 형태로 반환.
    """

    kw_main, kw_info, kw_long = selected_keywords

    user_prompt = f"""
[블로그 글 작성 요청]

- 기본 주제 키워드: {base_keyword}
- 자동 선정된 핵심 키워드 3개:
  1) 메인 키워드: {kw_main}
  2) 정보형 키워드: {kw_info}
  3) 롱테일 키워드: {kw_long}

요청사항:
- 네이버 블로그용으로 작성한다.
- 말투는 '~했어요', '~입니다' 정도의 담백한 1인칭 설명형.
- 제목, 도입부, ①~⑤, 마무리, 해시태그까지 지침서 구조를 정확히 지킨다.
- 세 키워드를 제목과 본문, 해시태그에 자연스럽게 분산해서 넣는다.
- 해시태그는 # 없이 문자열로만, 쉼표로 구분해서 한 줄에 쓴다.
- HTML은 쓰지 말고, 순수 텍스트/마크다운으로만 작성한다.
"""

    response = client.chat.completions.create(
        model="gpt-4.1-mini",
        messages=[
            {"role": "system", "content": GUIDELINE},
            {"role": "user", "content": user_prompt},
        ],
        temperature=0.6,
    )

    return response.choices[0].message.content.strip()


# -----------------------------
# 3. 화면 UI
# -----------------------------
base_kw = st.text_input(
    "분석할 키워드를 입력해주세요 😊",
    placeholder="예: 전주 덕진공원 연꽃",
)

col_btn, _ = st.columns([1, 5])
with col_btn:
    if st.button("키워드 추천 및 분석하기"):
        if not base_kw.strip():
            st.warning("키워드를 먼저 입력해주세요.")
        else:
            with st.spinner("GPT 기반 키워드 분석 중입니다..."):
                kw_table, top10_df, top3 = generate_keywords_with_gpt(base_kw.strip())

            st.session_state.analysis_done = True
            st.session_state.base_keyword = base_kw.strip()
            st.session_state.kw_table = kw_table
            st.session_state.top10_df = top10_df
            st.session_state.top3_keywords = top3

# -----------------------------
# 4. 분석 결과 표시
# -----------------------------
if st.session_state.analysis_done and st.session_state.kw_table is not None:
    st.markdown("### 1️⃣ 연관 키워드 50개 생성")
    st.caption("정렬 기준: 주제 연관성 + 검색 의도")
    st.dataframe(st.session_state.kw_table, use_container_width=True)

    st.markdown("### 2️⃣ 상위 노출 가능성이 높은 키워드 10개")
    st.dataframe(st.session_state.top10_df, use_container_width=True)

    st.markdown("### 3️⃣ 글 생성용 핵심 키워드 3개 (자동 선정)")
    k1, k2, k3 = st.session_state.top3_keywords
    st.write(f"- ① 메인 키워드: **{k1}**")
    st.write(f"- ② 정보형 키워드: **{k2}**")
    st.write(f"- ③ 롱테일 키워드: **{k3}**")

    st.info("위 3개 키워드는 검색량·의도·경쟁도를 기준으로 자동 선정된 조합입니다.")

    if st.button("이 3개 키워드로 네이버 블로그 글 자동 생성"):
        with st.spinner("네이버 블로그용 글을 생성하는 중입니다..."):
            post_text = generate_naver_post(
                st.session_state.base_keyword,
                st.session_state.top3_keywords,
            )
        st.markdown("## ✏️ 지침서 기반 글 뼈대 + 본문")
        st.markdown(post_text)
else:
    st.caption("키워드를 입력하고 '키워드 추천 및 분석하기' 버튼을 눌러주세요.")
